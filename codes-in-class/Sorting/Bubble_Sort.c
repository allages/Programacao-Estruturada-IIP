#include <stdio.h>

void bubbleSort(double v[], int n) {
    int i, fim, pos, troca;
    troca = 1; fim = n-2; pos = 0;
    while (troca == 1) {
        troca = 0;
        for (i = 0; i <= fim; i += 1){
            if (v[i] > v[i+1]){
            }
        }
    }